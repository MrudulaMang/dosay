
# git reset --soft HEAD~1
    #to delete last commit which i accidentaly commited twice
# git push --force origin plain-dosa
  # after deleteing the second commit, should make current commit and git origin/branch commit same
# git cat-file -p <commit id>
    #shows the tree